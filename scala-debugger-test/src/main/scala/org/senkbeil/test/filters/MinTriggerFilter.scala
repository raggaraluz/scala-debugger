package org.senkbeil.test.filters

/**
 * Used to verify the MinTriggerFilter.
 *
 * Breakpointable lines are 9, 10, 11, 12, 13, 15.
 */
object MinTriggerFilter extends App {
  val a = 0
  val b = 1
  val c = 2
  val d = 3
  val e = 4

  while (true) { Thread.sleep(1000) }
}
