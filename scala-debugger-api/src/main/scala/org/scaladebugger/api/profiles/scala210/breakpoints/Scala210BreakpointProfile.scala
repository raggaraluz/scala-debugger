package org.scaladebugger.api.profiles.scala210.breakpoints
import acyclic.file

import org.scaladebugger.api.profiles.pure.breakpoints.PureBreakpointProfile

/**
 * Represents a profile for breakpoints that adds logic specifically for
 * Scala 2.10.
 */
trait Scala210BreakpointProfile extends PureBreakpointProfile
