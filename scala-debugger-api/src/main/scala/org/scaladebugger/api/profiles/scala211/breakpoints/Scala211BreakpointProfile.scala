package org.scaladebugger.api.profiles.scala211.breakpoints
import acyclic.file

import org.scaladebugger.api.profiles.pure.breakpoints.PureBreakpointProfile

/**
 * Represents a profile for breakpoints that adds logic specifically for
 * Scala 2.11.
 */
trait Scala211BreakpointProfile extends PureBreakpointProfile
