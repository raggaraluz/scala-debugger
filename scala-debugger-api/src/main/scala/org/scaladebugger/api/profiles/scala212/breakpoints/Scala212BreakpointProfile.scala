package org.scaladebugger.api.profiles.scala212.breakpoints
import acyclic.file

import org.scaladebugger.api.profiles.pure.breakpoints.PureBreakpointProfile

/**
 * Represents a profile for breakpoints that adds logic specifically for
 * Scala 2.12.
 */
trait Scala212BreakpointProfile extends PureBreakpointProfile
